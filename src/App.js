import React from 'react';
import {SafeAreaView, StyleSheet, Text} from 'react-native';

export default function App() {
  return (
    <SafeAreaView>
      <Text>Emoji Search!</Text>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({});
